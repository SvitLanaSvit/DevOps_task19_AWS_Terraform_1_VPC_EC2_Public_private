# Step 8: Тестування SSH підключення та мережевої архітектури

## 🎯 Мета
Протестувати повну функціональність створеної VPC інфраструктури:
- SSH доступ до публічного EC2 (Jump Host)
- SSH доступ до приватного EC2 через Jump Host
- Інтернет доступ з обох серверів
- Перевірка Security Groups та мережевої ізоляції

## 🏗️ Архітектура тестування

```
🌐 Internet (ваш ПК)
    │
    │ SSH Test 1: Прямий доступ
    ▼
┌─────────────────────────────┐
│     Public Subnet           │
│   (10.0.1.0/24)             │
│                             │
│  ┌─────────────────────┐    │
│  │   Jump Host         │    │ ✅ SSH успішний
│  │ 🌐 18.199.90.158    │◄──┼─── ssh -i ~/.ssh/id_rsa ubuntu@18.199.90.158
│  │ 🏠 10.0.1.243       │   │
│  └─────────────────────┘    │
└─────────────┬───────────────┘
              │ SSH Test 2: Через Jump Host
              ▼
┌─────────────────────────────┐
│     Private Subnet          │
│   (10.0.2.0/24)             │
│                             │
│  ┌─────────────────────┐    │
│  │   Backend Server    │    │ 🔄 Тестуємо наступним
│  │ 🏠 10.0.2.114       │◄──┼─── SSH тільки через Jump Host
│  └─────────────────────┘    │
└─────────────────────────────┘
```

## 🧪 Тести для виконання

### ✅ Test 1: SSH до Jump Host (Публічний EC2)
**Статус**: ✅ **ПРОЙДЕНО УСПІШНО**

**Команда**:
```bash
ssh -i ~/.ssh/id_rsa ubuntu@18.199.90.158
```

**Очікуваний результат**: ✅ Успішне підключення до Ubuntu сервера
**Фактичний результат**: ✅ Підключення успішне

**Що перевіряється**:
- Internet Gateway працює правильно
- Public Subnet має доступ до інтернету
- Security Group дозволяє SSH з інтернету
- SSH ключ налаштований правильно
- EC2 instance запущений і доступний

### ✅ Test 2: SSH до Backend Server (Приватний EC2)
**Статус**: ✅ **ПРОЙДЕНО УСПІШНО**

**Метод 1 - Через ProxyCommand** ✅:
```bash
ssh -i ~/.ssh/id_rsa -o ProxyCommand="ssh -i ~/.ssh/id_rsa -W %h:%p ubuntu@18.199.90.158" ubuntu@10.0.2.114
```
**Результат**: ✅ Підключення успішне через Jump Host

**Метод 2 - Через Jump Host напряму** ✅:
```bash
# 1. Підключитися до Jump Host
ssh -i ~/.ssh/id_rsa ubuntu@18.199.90.158

# 2. З Jump Host підключитися до приватного сервера
ssh ubuntu@10.0.2.114
```
**Результат**: ✅ Підключення успішне

**Що перевіряється**:
- Private Subnet ізольований від інтернету
- Security Group блокує прямий доступ з інтернету
- SSH доступ працює тільки через Jump Host
- Приватний EC2 доступний з публічної підмережі

### ✅ Test 3: Негативний тест - Прямий SSH до приватного сервера
**Статус**: ✅ **ПРОЙДЕНО УСПІШНО (блокування працює)**

**Команда (має НЕ працювати)**:
```bash
ssh -i ~/.ssh/id_rsa ubuntu@10.0.2.114
```

**Очікуваний результат**: ❌ Відмова підключення (timeout або connection refused)
**Фактичний результат**: ✅ Підключення заблоковано - Security Group працює правильно!

**Що підтверджується**:
- Security Group приватного сервера блокує прямий доступ з інтернету
- Доступ можливий тільки з публічної підмережі (10.0.1.0/24)
- Мережева ізоляція працює як заплановано

### ✅ Test 4: Інтернет доступ з приватного сервера через Jump Host + Ping тести
**Статус**: ✅ **ПРОЙДЕНО УСПІШНО**

**Крок 1 - Підключення до Jump Host**:
```bash
ssh -i ~/.ssh/id_rsa ubuntu@18.199.90.158
```
**Результат**: ✅ Підключення до Jump Host успішне (IP: 10.0.1.243)

**Крок 2 - З Jump Host підключення до приватного сервера**:
```bash
# Вже на Jump Host, підключаємося до приватного сервера
ssh ubuntu@10.0.2.114
```
**Результат**: ✅ Підключення до приватного сервера (10.0.2.114) успішне

**Крок 3 - Ping тести з приватного сервера**:
```bash
# На приватному сервері, тестуємо інтернет доступ через NAT Gateway
ping -c 4 8.8.8.8

# Тест DNS резолюції + connectivity  
ping -c 4 google.com
```

**Результати ping тестів**:
- ✅ **ping 8.8.8.8**: Успішно (IP connectivity через NAT Gateway)
- ✅ **ping google.com**: Успішно (DNS резолюція + connectivity працюють)

**Альтернативний метод (одною командою через ProxyCommand)**:
```bash
# Прямий доступ до приватного сервера через ProxyCommand
ssh -i ~/.ssh/id_rsa -o ProxyCommand="ssh -i ~/.ssh/id_rsa -W %h:%p ubuntu@18.199.90.158" ubuntu@10.0.2.114

# Потім на приватному сервері виконати ping тести
ping -c 4 8.8.8.8
ping -c 4 google.com
```

**Що підтверджується**:
- ✅ NAT Gateway працює правильно для вихідного трафіку
- ✅ Приватний сервер має доступ до інтернету через NAT
- ✅ DNS резолюція працює (Route 53 resolver доступний)  
- ✅ Зовнішній трафік йде через NAT Gateway IP: 3.124.200.138
- ✅ Приватна підмережа має вихідний інтернет доступ але залишається ізольованою для вхідного трафіку
- ✅ Jump Host архітектура працює в обох режимах: покроковому та через ProxyCommand
- ✅ Security Groups перевірка вже включена в Test 3 (негативний тест прямого доступу)

## 📝 Протокол тестування

### Test 1 Results ✅ - SSH до Jump Host
- **Дата/Час**: 09.11.2025 16:46
- **Команда**: `ssh -i ~/.ssh/id_rsa ubuntu@18.199.90.158`
- **Результат**: ✅ Успішно
- **Деталі**: Підключення пройшло без помилок, Jump Host доступний
- **IP адреса всередині**: 10.0.1.243 (правильна приватна IP)

**Скриншот**: [Screens/12.1_result_ssh_public_es2.png]

### Test 2 Results ✅ - SSH до Backend через Jump Host
- **Дата/Час**: 09.11.2025 16:50
- **Команда 1**: `ssh -i ~/.ssh/id_rsa -o ProxyCommand="ssh -i ~/.ssh/id_rsa -W %h:%p ubuntu@18.199.90.158" ubuntu@10.0.2.114`
- **Результат 1**: ✅ ProxyCommand підключення успішне
- **Команда 2**: З Jump Host `ssh ubuntu@10.0.2.114`
- **Результат 2**: ✅ Пряме підключення з Jump Host успішне
- **Деталі**: Jump Host архітектура працює правильно

**Скриншот**: [Screens/12.2_result_ssh_private_es2_with_jump_host.png]

### Test 3 Results ✅ - Негативний тест Security Groups
- **Дата/Час**: 09.11.2025 16:52  
- **Команда**: `ssh -i ~/.ssh/id_rsa ubuntu@10.0.2.114` (прямий доступ)
- **Результат**: ✅ Правильно заблоковано
- **Деталі**: Security Group блокує прямий доступ з інтернету до приватного сервера
- **Підтвердження**: Мережева ізоляція працює як заплановано

**Скриншот**: [Screens/12.3_result_ssh_private_es2_without_jump_host.png]

### Test 4 Results ✅ - Інтернет доступ через NAT Gateway
- **Дата/Час**: 09.11.2025 16:55
- **Підключення**: `ssh -i ~/.ssh/id_rsa -o ProxyCommand="ssh -i ~/.ssh/id_rsa -W %h:%p ubuntu@18.199.90.158" ubuntu@10.0.2.114`
- **Ping 8.8.8.8**: ✅ Успішно (IP connectivity через NAT)
- **Ping google.com**: ✅ Успішно (DNS резолюція + connectivity)
- **NAT Gateway IP**: 3.124.200.138 (підтверджено)
- **Деталі**: Приватний сервер має вихідний інтернет доступ через NAT Gateway

**Скриншот**: [Screens/12.4_result_ssh_private_es2_ping_after_jump_host.png]
**Скриншот**: [Screens/12.5_result_ssh_private_es2_ping_after_jump_host.png]

### Test 5 Results 🔄
- **Security Groups Test**: [Очікує]
- **Negative Tests**: [Буде вказано]
- **Positive Tests**: [Буде вказано]
- **Скріншот**: `12.5_security_validation.png`

## 🛠️ Команди для швидкого тестування

### Копіювати SSH ключ на Jump Host
```bash
# На вашому ПК - копіюємо приватний ключ на Jump Host
scp -i ~/.ssh/id_rsa ~/.ssh/id_rsa ubuntu@18.199.90.158:~/.ssh/
```

### Встановлення прав на Jump Host
```bash
# На Jump Host
chmod 600 ~/.ssh/id_rsa
```

### Альтернативний метод - SSH Agent Forwarding
```bash
# Запустити SSH agent
eval $(ssh-agent)
ssh-add ~/.ssh/id_rsa

# Підключення з forwarding
ssh -A ubuntu@18.199.90.158

# З Jump Host до Backend (agent forwarding активний)
ssh ubuntu@10.0.2.114
```

## 🔍 Діагностика проблем

### Якщо SSH до Jump Host не працює:
```bash
# Перевірити статус instance
aws ec2 describe-instances --instance-ids i-0d5cbea86d242711e --query 'Reservations[].Instances[].[State.Name]'

# Перевірити Security Group
aws ec2 describe-security-groups --group-ids sg-097e16fe4c8bcc4bf

# Verbose SSH для діагностики
ssh -v -i ~/.ssh/id_rsa ubuntu@18.199.90.158
```

### Якщо SSH до Backend Server не працює:
```bash
# Перевірити з Jump Host чи доступний приватний сервер
ping 10.0.2.114
telnet 10.0.2.114 22

# Перевірити Security Group приватного сервера
aws ec2 describe-security-groups --group-ids sg-0c9acb2f6a65aedf9
```

### Якщо інтернет не працює:
```bash
# Перевірити Route Tables
aws ec2 describe-route-tables --filters "Name=vpc-id,Values=vpc-0d0c0723032da8852"

# Перевірити NAT Gateway статус
aws ec2 describe-nat-gateways --nat-gateway-ids nat-0f9609d5059dbc665
```

## ✅ Критерії успішного проходження

### ✅ Test 1: SSH до Jump Host
- Успішне підключення без помилок
- Отримання shell prompt
- Можливість виконувати команди

### ✅ Test 2: SSH до Backend через Jump Host  
- Успішне підключення до 10.0.2.114
- Підключення можливе тільки через Jump Host
- Прямий доступ з інтернету заблокований

### ✅ Test 3: Security Groups ізоляція (негативний тест)
- Прямий доступ до приватного сервера заблокований
- Security Groups правильно налаштовані
- Мережева ізоляція працює

### ✅ Test 4: Internet з Backend через NAT Gateway
- Ping до 8.8.8.8 та google.com успішний з приватного сервера
- DNS резолюція працює
- Вихідний трафік через NAT Gateway (IP: 3.124.200.138)

## 📸 Скріншоти для документації

**Необхідні скріншоти:**
1. `12.1_ssh_jump_host_success.png` - Успішне SSH до Jump Host
2. `12.2_ssh_backend_success.png` - SSH до Backend через Jump Host  
3. `12.3_security_isolation_test.png` - Security Groups перевірка (негативний тест)
4. `12.4_backend_internet_nat.png` - Internet тест з Backend (NAT Gateway)

## 🎉 ПІДСУМОК ТЕСТУВАННЯ

### ✅ **ВСІ ТЕСТИ ПРОЙДЕНІ УСПІШНО!**

**Протестовано та підтверджено:**
1. ✅ **Jump Host доступність** - SSH з інтернету працює
2. ✅ **Приватний сервер доступність** - SSH тільки через Jump Host (ProxyCommand)  
3. ✅ **Security Groups ізоляція** - прямий доступ заблокований (негативний тест)
4. ✅ **NAT Gateway функціональність** - вихідний інтернет доступ працює
5. ✅ **DNS резолюція** - ping google.com успішний через NAT Gateway
6. ✅ **Мережева архітектура** - повна VPC інфраструктура функціональна

**Архітектура Jump Host + приватна підмережа працює ідеально!** 🚀

## 🔄 Наступні кроки

➡️ **Step 9:** Очистка ресурсів після успішного тестування
- ✅ Документація результатів тестування завершена
- 🔄 `terraform destroy` для видалення всіх ресурсів  
- 🔄 Підтвердження очистки в AWS Console
- 🔄 Фінальний commit з результатами тестування